import React from 'react';
import '../styles/AbstractBackground.css'; // Import the CSS file

const AbstractBackground = () => {
  return (
    <div className="abstract-background"></div>
  );
};

export default AbstractBackground;
